<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Мой Магазин</title>
    <style>
        body { background: #1c2732; color: white; font-family: sans-serif; text-align: center; padding-top: 50px; }
        .card { background: #242f3d; padding: 20px; border-radius: 15px; display: inline-block; }
        button { background: #5ea8e8; border: none; padding: 10px 20px; color: white; border-radius: 5px; cursor: pointer; }
    </style>
</head>
<body>
    <div class="card">
        <h1>💎 Приватка OSINT</h1>
        <p>Цена: 5 NFT</p>
        <form action="/buy/Privatka" method="post">
            <button type="submit">КУПИТЬ</button>
        </form>
    </div>
</body>
</html>
